####### data is in employee.json and employee.csv 
##### Now 
# Uses Path to create the CSV path.
# Checks whether the CSV exists.
# Reads the CSV using csv.DictReader.
# Prints every employee's name.
# Uses Path to create the JSON path.
# Reads the JSON using json.load().
# Prints the company name.
# Prints every employee's department.
# Uses glob("*.csv") to find all CSV files inside data.


